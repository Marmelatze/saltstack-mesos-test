description "Marathon scheduler for Mesos"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

script

  LIBPROCESS_IP={{ grains['ip_interfaces'][pillar['mesos']['interface']][0] }}
  /usr/bin/marathon --ha --hostname {{ grains['fqdn'] }}

end script
