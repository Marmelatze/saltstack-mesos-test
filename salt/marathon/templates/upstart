description "Marathon scheduler for Mesos"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

exec /usr/bin/marathon --ha --hostname {{ salt['grains.get']('ip4_interfaces:eth0:0', '127.0.0.1') }}
